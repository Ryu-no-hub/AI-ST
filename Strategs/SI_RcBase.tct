
OpenTactic("SI Resource Base", CIV_SI, 10);
  // Parameters

  // Fleets
  include<[_curr_path]\SI_BaseRC.flt>; 
  include<[_curr_path]\SI_Builder.flt>; 
  include<[_curr_path]\SI_Transport_Corium.flt>; 
CloseTactic();
