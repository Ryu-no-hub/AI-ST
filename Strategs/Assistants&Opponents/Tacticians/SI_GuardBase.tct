
OpenTactic("SI Guard Base", CIV_SI, 10);
  // Parameters

  // Fleets
  include<[_curr_path]\Fleets\SI_BaseGuard.flt>; 
  include<[_curr_path]\Fleets\SI_Builder.flt>; 
  include<[_curr_path]\Fleets\SI_Transport_Corium.flt>; 
  include<[_curr_path]\Fleets\SI_Guard_00.flt>; 
  include<[_curr_path]\Fleets\SI_Guard_01.flt>; 
CloseTactic();
